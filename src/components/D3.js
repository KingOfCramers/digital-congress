import React from "react";

export default class Visualization extends React.Component {
    render(){
        return (
            <div>
                <svg id="viz"></svg>
            </div>
        );
    }
}